{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.clientButton({
    'dataUrl': path('app_issue_add_dialog', { 'id': entity.id }),
    'aCss': 'no-hash',
    'iCss': 'icon-plus-sign',
    'dataId': entity.id,
    'title' : 'app.add_dialog.title'|trans,
    'label' : 'app.add_dialog.button_label'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : false,
        'reload-grid-name' : 'user-view-page-issues-grid',
        'options' : {
            'stateEnabled': false,
            'dialogOptions' : {
                'title' : 'app.add_dialog.title'|trans,
                'allowMaximize': false,
                'allowMinimize': false,
                'width': 600
            }
        }
    }
}) }}